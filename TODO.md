# TODOs

- optional show file size in project explorer

- cleanup code, get claude to find duplicate, unused etc code snippets

- internal/external function: do we index only once?
   what the best if we write a new function?

- still ctrl-hoover sometimes opens external func link



again: no improvements at all:

- still there: popup on ctrl-hoover on all elements
  popup now different content for internal functions ,
  for variables still the same "multiple implementations"
  see screenshots

---

Settings include path -> copy path feature

----

Actually expected is unix style, make sure you use OS specific file separator everywhere.
